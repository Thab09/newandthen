import React from "react";
import { Toggle } from "../styles/LogoBar.styled";

function LogoBar() {
  return (
    <div>
      <h1>now&then</h1>
      {/* <Toggle onClick={changeTheme}>{icon}</Toggle> */}
    </div>
  );
}

export default LogoBar;
